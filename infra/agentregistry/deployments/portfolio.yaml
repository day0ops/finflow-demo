apiVersion: ar.dev/v1alpha1
kind: Deployment
metadata:
  name: finflow-portfolio
spec:
  targetRef:
    kind: Agent
    name: finflow-portfolio
    tag: "latest"
  runtimeRef:
    kind: Runtime
    name: agentcore
  env:
    LLM_BASE_URL: https://app.mesh-demo.kasunt.apac.fe.solo.io/openai/v1
    PORTFOLIO_MCP_URL: https://app.mesh-demo.kasunt.apac.fe.solo.io/finflow/portfolio
  runtimeConfig:
    region: ${AWS_REGION}
    workdir: agents/portfolio
    networkMode: vpc
    subnetIds:
      - ${SUBNET_ID}
    securityGroupIds:
      - ${SG_ID}
