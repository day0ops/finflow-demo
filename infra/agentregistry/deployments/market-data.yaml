apiVersion: ar.dev/v1alpha1
kind: Deployment
metadata:
  name: finflow-market-data
spec:
  targetRef:
    kind: Agent
    name: finflow-market-data
    tag: "latest"
  runtimeRef:
    kind: Runtime
    name: agentcore
  runtimeConfig:
    region: ${AWS_REGION}
    workdir: agents/market-data
    networkMode: vpc
    subnetIds:
      - ${SUBNET_ID}
    securityGroupIds:
      - ${SG_ID}
