apiVersion: ar.dev/v1alpha1
kind: Deployment
metadata:
  name: finflow-news-sentiment
spec:
  targetRef:
    kind: Agent
    name: finflow-news-sentiment
    tag: "latest"
  runtimeRef:
    kind: Runtime
    name: agentcore
  runtimeConfig:
    region: ${AWS_REGION}
    workdir: agents/news-sentiment
    networkMode: vpc
    subnetIds:
      - ${SUBNET_ID}
    securityGroupIds:
      - ${SG_ID}
